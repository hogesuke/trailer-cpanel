import * as api from '../api';
import * as types from './mutation-types';

// TODO: あとで消す
// export const fetch = ({ commit }) => {
//   api.fetchPlans(data => {
//     commit(types.FUGA, { data });
//   });
// };
